namespace RDTS.NodeEditor
{
    // MessageType is only available in UnityEditor so we have our own for runtime
    //MessageType 仅在 UnityEditor 中可用，因此我们有自己的运行时
    /// <summary>
    /// 四种消息类型：None，Info，Warning，Error
    /// </summary>
    public enum NodeMessageType
    {
        None,
        Info,
        Warning,
        Error
    }








}